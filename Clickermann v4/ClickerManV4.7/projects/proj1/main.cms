// Тестовый скрипт для демонстрации принципа "папки проекта"

// Основной скрипт проекта


#name "my program"
#include "find"

// в подключаемом файле звук загрузки, попап сообщение и вызов плагина


getscreen

// поиск значка в трее по картинке из папки проекта
if_picture_in(0,0, $_xmax, $_ymax, "cman_ico.bmp", 16711935)
  move($_return1+8, $_return2+8)
  wait(1)
  hintpopup("кликер вот тут!")
end_if